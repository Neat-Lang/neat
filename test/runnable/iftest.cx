module iftest;

macro import cx.macros.assert;

void main() {
    int i = 0;
    if (1 == 1) i = 1;
    assert(i == 1);
    if (1 == 1) i = 1; else i = 2;
    assert(i == 1);
    if (1 == 2) i = 1; else i = 2;
    assert(i == 2);
    if (!(1 == 1)) assert(false);
    if (1 != 1) assert(false);
}
