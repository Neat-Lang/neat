module tuples;

macro import cx.macros.assert;

void main() {
    (int, float) tuple = (2, 3.0);
    assert(tuple[0] == 2 && tuple[1] == 3.0);
    (int, float) fun() { return (2, 3.0); }
    // TODO
    // assert(tuple == fun);
}
